/**
 * Optional helper — use when you want the package-exported viem chains directly:
 *   import { arc, arcTestnet } from "viem/chains";
 * AgentTill overlays authoritative RPCs in `./chains`.
 */
export { arc, arcTestnet } from "viem/chains";
